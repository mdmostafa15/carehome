export default function NotFoundComp (params) {
    return (
        <div style={{
            textAlign:"center",
            padding: "48px 0",
            color: "red"
        }}>
            <h3>Your care home not found!</h3>
            <p>we are sill working on ...</p>
        </div>
    )
}